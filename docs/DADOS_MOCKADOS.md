# Dados mockados/placeholder ainda em produção

> Inventário gerado em 2026-08-23 lendo o código real (não as specs), a pedido do
> usuário, para dar visibilidade a todo ponto da UI que hoje exibe dado
> não-real ou uma feature stub. Nenhum destes é um mock silencioso — todos já
> estão isolados atrás de um serviço/config nomeado e comunicam seu estado
> honestamente na UI (regra 2 da constituição, `specs/constitution.md`). Nenhum
> item aqui deve ser "consertado" sem decisão prévia do usuário/orientador —
> ver coluna "Decisão pendente".

| # | Tela(s) | Arquivo(s) | O que é mockado hoje | O que deveria estar lá | Decisão pendente? |
|---|---|---|---|---|---|
| 1 | Assistente de IA (4a) | `src/services/aiAssistantService.ts:27-42` | `sendMessage()` sorteia uma de 4 respostas fixas em `MOCK_RESPONSES`, com um delay artificial de 1-2s simulando processamento. A UI/hook (`useChatBot.ts`) não sabem que é mock — só conhecem o contrato `AiAssistantService`. | Chamada real a um provedor de LLM (esboço de integração com Claude API já deixado em comentário no próprio arquivo). | **Sim** — provedor, custo por token e política de retenção/privacidade de dados de saúde enviados a terceiro (LGPD) precisam de confirmação do usuário/orientador antes de qualquer implementação. |
| 2 | Assistente de IA — drawer de histórico (4a) | `src/hooks/useChatBot.ts:38,118` | `historyGroups` é inicializado e permanece **sempre** um array vazio — a UI do drawer (`HistoryDrawer.tsx`) está pronta, mas nenhuma conversa passada é de fato recuperável (o estado vive só em `useState`, perdido ao fechar o app). | Persistência real de conversas — local (AsyncStorage/SecureStore) ou um novo model Amplify (`ChatConversation`/`ChatMessage` no DynamoDB). | **Sim** — a segunda rota é mudança de schema e cria uma nova superfície de dados de saúde sensíveis sob LGPD; nenhuma das duas rotas foi escolhida ainda. |
| 3 | Home — card "Prevenção em atraso" (2b) | `src/app/(app)/dashboard.tsx:100` | `preventionAlert={null}` é passado fixo, sempre — o card correspondente em `HomeScreen.tsx` (`PreventionAlertCard`) nunca renderiza. Não é um dado falso exibido, é uma ausência deliberada. | O item de prevenção "mais atrasado" vindo de Prevenção (3e). | **Sim, indiretamente** — não é só uma questão de conexão: `PreventionRecommendation` (`src/types/models.ts:244`) não tem nenhum conceito de "atrasado" (sem due date), só grau USPSTF (A-I). Definir o que torna uma recomendação "atrasada" é uma regra de negócio nova, não uma mudança de UI. |
| 4 | Perfil — "Dispositivos conectados" (4b) | `src/services/health/healthAppConnectService.ts` | `getHealthConnectStatus()` sempre retorna `'unavailable'`; a tela mostra "Indisponível" (`ProfileScreen.tsx:312`) e um alerta "Em breve você poderá conectar o Apple Health ou Google Fit por aqui" (`ProfileScreen.tsx:102`). | Integração real com HealthKit (iOS) / Health Connect ou Google Fit (Android). | **Sim** — exige escolher e instalar uma lib nativa nova (regra 3 da constituição) e desenhar o fluxo de permissão/consentimento de dados de saúde de terceiros. |
| 5 | Perfil — "Exportar meus dados" (4b) | `src/services/export/dataExportService.ts` | `requestDataExport()` sempre retorna `'unavailable'`; a tela mostra um alerta "Em breve. Para solicitar seus dados agora, contate o suporte" (`ProfileScreen.tsx:95`). | Mecanismo real de geração/entrega de exportação (requisito de portabilidade da LGPD, não só preferência de UI). | **Sim** — formato do export (JSON/PDF/CSV), transporte (e-mail assinado, download direto, etc.) e prazo de atendimento não foram definidos. |
| 6 | Agenda — "Sincronizar com Google Agenda" (2c) | `src/services/googleCalendarSync.ts` | `isGoogleCalendarSyncAvailable()` sempre retorna `false`; o botão abre um modal local "Em breve" em vez de sincronizar de fato. Nenhum client OAuth/Calendar API existe no repo (o único uso de "Google" hoje é login via Cognito/Google Identity Provider, não a Calendar API). | Integração real via Google Calendar API: OAuth com escopo `calendar`, backend de refresh token, sync bidirecional. | **Sim** — é uma EPIC própria (OAuth + backend de token), não decidida/priorizada ainda. |
| 7 | Prevenção (3e) e Carteira de Vacinação (4e) — banner de campanha | `src/config/vaccinationCampaigns.ts:19-27` | `VACCINATION_CAMPAIGNS` é um array estático hardcoded no código-fonte (hoje só a campanha de gripe 2026), mantido manualmente — não vem de nenhuma API pública. O texto exibido ao usuário ("Campanha de vacinação contra a gripe até 30/09...") é real/institucional, mas a fonte é config, não dado dinâmico. | Integração com fonte pública de calendário de campanhas (ex. API do PNI/Ministério da Saúde). | **Sim** — qual fonte pública usar (se existir uma API estável) não foi pesquisado/decidido. |
| 8 | Home — sino de notificações (2b) | `src/screens/HomeScreen.tsx:43,93` | O ícone de sino é renderizado como elemento visual estático; `onNotificationPress` é opcional e hoje não é passado por `dashboard.tsx` (no-op). Não existe badge de não-lido nem painel — porque não existe nenhum model, serviço ou tela de notificações em nenhum lugar do código. | Sistema de notificações real: model de notificação (ex. DynamoDB), serviço de contagem de não-lidas, tela/painel de central de notificações. | **Sim** — feature inteiramente ausente, sem dono de Bloco definido, escopo (o que gera uma notificação?) não decidido. |
| 9 | Exames — filtro "Alterados" (3a) | `src/components/FilterChips.tsx:12,56` | O chip "Alterados" aparece desabilitado com sufixo " · Em breve" — decisão correta e já documentada (não finge ter dados), mas vale registrar aqui: não existe hoje nenhum campo de resultado clínico ("normal"/"alterado") no schema `MedicalDocument`. | Um campo real de status/resultado clínico no documento, preenchido por quem? (usuário, IA, integração com laboratório) — não definido. | **Sim** — depende de definir a fonte desse dado clínico, fora do escopo do MVP atual. |

## Observações

- Todos os 9 itens já seguem o padrão da constituição (`specs/constitution.md`, regra 2): nenhum finge sucesso silenciosamente — cada um comunica seu estado real na UI ("Indisponível", "Em breve", ausência do card) e está isolado atrás de uma função/serviço com nome explícito, pronta para virar um simples swap quando a decisão for tomada.
- Os itens 1, 2, 4, 5, 6, 7 e 8 já estão listados como pendências técnicas numeradas em `specs/design/GAP_ANALYSIS.md` (respectivamente #4.a/#4.b, #26, #26, #9.b/#13, #3.a, #9.a/#14). Este documento não substitui o `GAP_ANALYSIS.md` — é um resumo focado especificamente em "onde está o dado falso/stub e o que deveria estar lá", para consulta rápida sem precisar ler o gap analysis inteiro.
- Nenhum destes 9 itens foi alterado nesta sessão — por instrução explícita do usuário, nenhuma decisão de projeto foi tomada agora. Ver `specs/RELATORIO_SDD_SUASAUDE.md` para o histórico de decisões já confirmadas em sessões anteriores.
